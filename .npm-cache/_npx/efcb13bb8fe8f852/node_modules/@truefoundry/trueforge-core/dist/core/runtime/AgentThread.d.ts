import { type AgentInfo, type AgentParent } from '../events/schema';
import type { AgentDefinition } from './AgentDefinition';
import type { AgentThreadConstructorInput, AgentThreadRuntimeSendBatch, AgentThreadSnapshot } from './AgentThread.types';
import { type AgentThreadAppendContext, type AgentThreadEvent } from './AgentThread.types';
import { type AgentThreadMetrics } from './metrics';
export declare class AgentThread {
    readonly threadId: string;
    readonly parent?: AgentParent | undefined;
    readonly definition: AgentDefinition;
    readonly title: string;
    readonly agentInfo?: AgentInfo | undefined;
    private context;
    private currentContextUsage;
    private preSendContextProcessors;
    private preLLMContextProcessors;
    private preEphemeralLLMContextProcessors;
    private postToolCallContextProcessor;
    private instruction;
    private toolResponseProcessors;
    private instructionBuilders;
    private systemToolSets;
    private deferredTool?;
    private convertedTools;
    private pendingSandboxCreatedEvents;
    private sandbox?;
    private readonly tracing;
    private readonly logger;
    private metrics;
    private tfyManagedServerNames;
    private contextBusy;
    private preSendRanThisTurn;
    private currentState;
    private readonly preComputedCompletion?;
    /** Mirrored capability KV — source for toSnapshot().capability_state. */
    private capabilityState;
    private readonly capabilityStateKeys;
    constructor(input: AgentThreadConstructorInput);
    private buildInstruction;
    send(messages: AgentThreadRuntimeSendBatch): AsyncGenerator<AgentThreadAppendContext, void, unknown>;
    private throwIfContextBusy;
    private deriveState;
    private appendToContext;
    private overwriteContext;
    private generateErrorEvent;
    hasOpenToolCallId(toolCallId: string): boolean;
    isAwaitingUserInput(): boolean;
    validateSendInput(messages: AgentThreadRuntimeSendBatch): void;
    toSnapshot(): AgentThreadSnapshot;
    getAgentThreadMetrics(): AgentThreadMetrics;
    private transformToLLMRequest;
    private computeAssistantMessageUsage;
    private executeContextProcessors;
    private init;
    private initTools;
    private getConvertedTools;
    private stepLLMCall;
    private stepToolResponse;
    private stepUserInputRequired;
    private buildReplayEvent;
    execute(options?: {
        signal?: AbortSignal | undefined;
    }): AsyncGenerator<AgentThreadEvent, void, unknown>;
}
//# sourceMappingURL=AgentThread.d.ts.map