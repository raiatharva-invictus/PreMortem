// src/db/postgres/migrations/20260811_000001_session_created_by.ts
import { sql } from "kysely";
async function up(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.alterTable("session").addColumn("created_by", "text", (col) => col.notNull().defaultTo("")).execute();
}
async function down(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.alterTable("session").dropColumn("created_by").execute();
}
export {
  down,
  up
};
//# sourceMappingURL=20260811_000001_session_created_by.js.map