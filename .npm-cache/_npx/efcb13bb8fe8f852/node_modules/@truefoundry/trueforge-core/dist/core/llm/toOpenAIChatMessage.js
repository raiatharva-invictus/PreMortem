"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/core/llm/toOpenAIChatMessage.ts
var toOpenAIChatMessage_exports = {};
__export(toOpenAIChatMessage_exports, {
  toOpenAIChatMessage: () => toOpenAIChatMessage
});
module.exports = __toCommonJS(toOpenAIChatMessage_exports);
function toOpenAIChatMessage(msg) {
  if (msg.role === "user") {
    return { role: "user", content: toOpenAIUserContent(msg.content) };
  }
  if (msg.role === "tool") {
    return {
      role: "tool",
      tool_call_id: msg.tool_call_id,
      content: msg.content
    };
  }
  if (!msg.tool_calls?.length) {
    const { tool_calls: _omit, ...rest } = msg;
    void _omit;
    return rest;
  }
  return {
    ...msg,
    tool_calls: msg.tool_calls.map(({ tool_info: _toolInfo, ...rest }) => {
      void _toolInfo;
      return rest;
    })
  };
}
function toOpenAIUserContent(content) {
  if (typeof content === "string") {
    return content;
  }
  const parts = [];
  for (const part of content) {
    if (part.type === "text") {
      parts.push({ type: "text", text: part.text });
      continue;
    }
    if (part.type === "image_url") {
      parts.push({
        type: "image_url",
        image_url: {
          url: part.image_url.url,
          ...part.image_url.detail !== void 0 ? { detail: part.image_url.detail } : {}
        }
      });
      continue;
    }
    if (part.type === "input_audio") {
      parts.push({
        type: "input_audio",
        input_audio: {
          data: part.input_audio.data,
          format: part.input_audio.format
        }
      });
      continue;
    }
    parts.push({
      type: "file",
      file: {
        ...part.file.file_data !== void 0 ? { file_data: part.file.file_data } : {},
        ...part.file.file_id !== void 0 ? { file_id: part.file.file_id } : {},
        ...part.file.filename !== void 0 ? { filename: part.file.filename } : {}
      }
    });
  }
  return parts;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  toOpenAIChatMessage
});
//# sourceMappingURL=toOpenAIChatMessage.js.map