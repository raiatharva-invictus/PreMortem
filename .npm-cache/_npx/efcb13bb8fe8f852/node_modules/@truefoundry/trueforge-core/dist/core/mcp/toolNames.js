"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/core/mcp/toolNames.ts
var toolNames_exports = {};
__export(toolNames_exports, {
  MAX_DUP_SUFFIX: () => MAX_DUP_SUFFIX,
  getUniqueSanitizedToolName: () => getUniqueSanitizedToolName
});
module.exports = __toCommonJS(toolNames_exports);
var import_errors = require("../errors.js");
var VALID_TOOL_NAME_PATTERN = /^[a-zA-Z0-9_-]+$/;
var MAX_TOOL_NAME_LENGTH = 64;
var MAX_DUP_SUFFIX = 50;
function sanitizeToolName(toolName) {
  if (VALID_TOOL_NAME_PATTERN.test(toolName)) {
    return toolName;
  }
  return toolName.replace(/[^a-zA-Z0-9_-]/g, "_");
}
function getUniqueSanitizedToolName(toolName, existingNames) {
  const sanitized = sanitizeToolName(toolName);
  const truncated = sanitized.substring(0, MAX_TOOL_NAME_LENGTH);
  if (!existingNames.has(truncated)) {
    return { name: truncated, hadCollision: false };
  }
  const nameBase = sanitized.substring(0, MAX_TOOL_NAME_LENGTH - 2);
  for (let suffix = 1; suffix <= MAX_DUP_SUFFIX; suffix++) {
    const candidate = `${nameBase}${String(suffix)}`;
    if (!existingNames.has(candidate)) {
      return { name: candidate, hadCollision: true };
    }
  }
  throw new import_errors.ToolNameCollisionError(
    `Tool name collision limit exceeded for "${toolName}": more than ${String(MAX_DUP_SUFFIX)} duplicates across MCP servers`
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  MAX_DUP_SUFFIX,
  getUniqueSanitizedToolName
});
//# sourceMappingURL=toolNames.js.map