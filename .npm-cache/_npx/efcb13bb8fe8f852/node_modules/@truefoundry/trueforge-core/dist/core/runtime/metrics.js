"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/core/runtime/metrics.ts
var metrics_exports = {};
__export(metrics_exports, {
  AgentThreadMetricsSchema: () => AgentThreadMetricsSchema,
  addAgentThreadMetrics: () => addAgentThreadMetrics,
  createEmptyAgentThreadMetrics: () => createEmptyAgentThreadMetrics,
  updateMetricsFromUsage: () => updateMetricsFromUsage
});
module.exports = __toCommonJS(metrics_exports);
var import_zod_openapi = require("@hono/zod-openapi");
var AgentThreadMetricsSchema = import_zod_openapi.z.object({
  total_input_tokens: import_zod_openapi.z.number().int().nonnegative(),
  total_output_tokens: import_zod_openapi.z.number().int().nonnegative(),
  total_tokens: import_zod_openapi.z.number().int().nonnegative(),
  total_cache_read_tokens: import_zod_openapi.z.number().int().nonnegative().optional(),
  total_cache_write_tokens: import_zod_openapi.z.number().int().nonnegative().optional(),
  total_reasoning_tokens: import_zod_openapi.z.number().int().nonnegative().optional(),
  total_cost_in_usd: import_zod_openapi.z.number().nonnegative().optional(),
  iterations: import_zod_openapi.z.number().int().nonnegative(),
  total_tool_calls: import_zod_openapi.z.number().int().nonnegative(),
  total_summarizations: import_zod_openapi.z.number().int().nonnegative(),
  total_sub_agents: import_zod_openapi.z.number().int().nonnegative()
}).openapi("AgentThreadMetrics");
function sumOptional(a, b) {
  if (a === void 0 && b === void 0) {
    return void 0;
  }
  return (a ?? 0) + (b ?? 0);
}
function createEmptyAgentThreadMetrics() {
  return {
    total_input_tokens: 0,
    total_output_tokens: 0,
    total_tokens: 0,
    iterations: 0,
    total_tool_calls: 0,
    total_summarizations: 0,
    total_sub_agents: 0
  };
}
function updateMetricsFromUsage(target, usage) {
  target.total_input_tokens += usage.input_tokens;
  target.total_output_tokens += usage.output_tokens;
  target.total_tokens += usage.total_tokens;
  target.total_cache_read_tokens = sumOptional(target.total_cache_read_tokens, usage.cache_read_tokens);
  target.total_cache_write_tokens = sumOptional(target.total_cache_write_tokens, usage.cache_write_tokens);
  target.total_reasoning_tokens = sumOptional(target.total_reasoning_tokens, usage.reasoning_tokens);
  target.total_cost_in_usd = sumOptional(target.total_cost_in_usd, usage.cost_in_usd);
}
function addAgentThreadMetrics(target, source) {
  target.total_input_tokens += source.total_input_tokens;
  target.total_output_tokens += source.total_output_tokens;
  target.total_tokens += source.total_tokens;
  target.total_cache_read_tokens = sumOptional(target.total_cache_read_tokens, source.total_cache_read_tokens);
  target.total_cache_write_tokens = sumOptional(target.total_cache_write_tokens, source.total_cache_write_tokens);
  target.total_reasoning_tokens = sumOptional(target.total_reasoning_tokens, source.total_reasoning_tokens);
  target.total_cost_in_usd = sumOptional(target.total_cost_in_usd, source.total_cost_in_usd);
  target.iterations += source.iterations;
  target.total_tool_calls += source.total_tool_calls;
  target.total_summarizations += source.total_summarizations;
  target.total_sub_agents += source.total_sub_agents;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AgentThreadMetricsSchema,
  addAgentThreadMetrics,
  createEmptyAgentThreadMetrics,
  updateMetricsFromUsage
});
//# sourceMappingURL=metrics.js.map