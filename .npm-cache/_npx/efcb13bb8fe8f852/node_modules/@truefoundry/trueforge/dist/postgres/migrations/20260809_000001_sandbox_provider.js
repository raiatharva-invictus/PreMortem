// src/db/postgres/migrations/20260809_000001_sandbox_provider.ts
import { sql } from "kysely";
async function up(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.createTable("sandbox_provider").addColumn("tenant_id", "text", (col) => col.notNull()).addColumn("manifest", "jsonb", (col) => col.notNull()).addColumn("created_at", "timestamptz", (col) => col.notNull()).addColumn("updated_at", "timestamptz", (col) => col.notNull()).addPrimaryKeyConstraint("sandbox_provider_pkey", ["tenant_id"]).execute();
}
async function down(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.dropTable("sandbox_provider").ifExists().cascade().execute();
}
export {
  down,
  up
};
//# sourceMappingURL=20260809_000001_sandbox_provider.js.map