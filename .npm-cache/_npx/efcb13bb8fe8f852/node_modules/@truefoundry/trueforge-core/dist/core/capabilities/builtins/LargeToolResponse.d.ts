import type { Logger } from 'winston';
import type { ToolCallResult } from '../../mcp/executeToolCalls';
import type { AgentCapability } from '../AgentCapability';
import type { AgentThreadExecutionContext } from '../AgentContextProcessor';
import type { ToolResponseProcessor, ToolResponseProcessorResult } from '../ToolResponseProcessor';
export declare const DEFAULT_INDIVIDUAL_TOOL_TOKEN_THRESHOLD = 6000;
export declare const DEFAULT_TOTAL_TOOL_TOKEN_THRESHOLD = 10000;
export declare const DEFAULT_PREVIEW_NUMBER_OF_CHARACTERS = 100;
export declare class LargeToolResponseProcessor implements ToolResponseProcessor {
    private dynamicSubAgentsPresent;
    private individualTokenThreshold;
    private totalTokenThreshold;
    private previewNumberOfCharacters;
    private readonly logger;
    constructor(param: {
        dynamicSubAgentsPresent: boolean;
        individualTokenThreshold: number;
        totalTokenThreshold: number;
        previewNumberOfCharacters: number;
        logger: Logger;
    });
    private createLargeToolResponseGuidance;
    private createFileDumpMessage;
    private buildResultsWithInfo;
    private blockResult;
    private uploadToSandboxAndPrependFilePath;
    private executeSandboxDumps;
    process(results: ToolCallResult[], execution: Readonly<AgentThreadExecutionContext>): Promise<ToolResponseProcessorResult>;
}
export interface LargeToolResponseSettings {
    individualTokenThreshold?: number | undefined;
    totalTokenThreshold?: number | undefined;
    previewNumberOfCharacters?: number | undefined;
}
export declare function largeToolResponse(options: {
    dynamicSubAgentsPresent: boolean;
    settings?: LargeToolResponseSettings | undefined;
    logger: Logger;
}): AgentCapability;
//# sourceMappingURL=LargeToolResponse.d.ts.map