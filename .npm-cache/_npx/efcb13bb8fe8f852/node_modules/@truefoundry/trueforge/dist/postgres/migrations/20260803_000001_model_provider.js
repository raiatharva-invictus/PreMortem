// src/db/postgres/migrations/20260803_000001_model_provider.ts
import { sql } from "kysely";
async function up(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.createTable("model_provider").addColumn("tenant_id", "text", (col) => col.notNull()).addColumn("name", "text", (col) => col.notNull()).addColumn("manifest", "jsonb", (col) => col.notNull()).addColumn("created_at", "timestamptz", (col) => col.notNull()).addColumn("updated_at", "timestamptz", (col) => col.notNull()).addPrimaryKeyConstraint("model_provider_pkey", ["tenant_id", "name"]).execute();
}
async function down(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.dropTable("model_provider").ifExists().cascade().execute();
}
export {
  down,
  up
};
//# sourceMappingURL=20260803_000001_model_provider.js.map