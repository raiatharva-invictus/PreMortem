// src/db/sqlite/migrations/20260809_000001_sandbox_provider.ts
import { sql } from "kysely";
async function up(db) {
  await db.transaction().execute(async (trx) => {
    await sql`
      CREATE TABLE sandbox_provider (
        tenant_id TEXT NOT NULL,
        manifest BLOB NOT NULL,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        PRIMARY KEY (tenant_id)
      ) STRICT
    `.execute(trx);
  });
}
async function down(db) {
  await db.transaction().execute(async (trx) => {
    await sql`DROP TABLE IF EXISTS sandbox_provider`.execute(trx);
  });
}
export {
  down,
  up
};
//# sourceMappingURL=20260809_000001_sandbox_provider.js.map