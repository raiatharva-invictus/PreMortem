"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/agent-session/builtinsFromSpec.ts
var builtinsFromSpec_exports = {};
__export(builtinsFromSpec_exports, {
  builtinsFromSpec: () => builtinsFromSpec
});
module.exports = __toCommonJS(builtinsFromSpec_exports);
var import_AskUserQuestion = require("../core/capabilities/builtins/AskUserQuestion.js");
var import_ContextCompaction = require("../core/capabilities/builtins/ContextCompaction.js");
var import_CurrentDateTime = require("../core/capabilities/builtins/CurrentDateTime.js");
var import_DynamicSubAgents = require("../core/capabilities/builtins/DynamicSubAgents.js");
var import_LargeToolResponse = require("../core/capabilities/builtins/LargeToolResponse.js");
var import_OpenUI = require("../core/capabilities/builtins/OpenUI.js");
function builtinsFromSpec(input) {
  const { spec, definition, isChild, sandboxAvailable, tracing, logger } = input;
  const config = spec.config;
  const capabilities = [(0, import_CurrentDateTime.currentDateTime)({ tracing })];
  if (config.context_management.compaction.enabled) {
    capabilities.push(
      (0, import_ContextCompaction.contextCompaction)({
        definition,
        compactionThresholdTokens: config.context_management.compaction.compaction_threshold_tokens
      })
    );
  }
  if (!isChild && config.ask_user_questions.enabled) {
    capabilities.push((0, import_AskUserQuestion.askUserQuestion)());
  }
  const dynamicSubAgentsEnabled = config.dynamic_sub_agents.enabled;
  if (!isChild && dynamicSubAgentsEnabled) {
    capabilities.push(
      (0, import_DynamicSubAgents.dynamicSubAgents)({
        sandboxAvailable,
        tracing
      })
    );
  }
  if (config.context_management.large_tool_response.enabled) {
    capabilities.push(
      (0, import_LargeToolResponse.largeToolResponse)({
        dynamicSubAgentsPresent: !isChild && dynamicSubAgentsEnabled,
        logger
      })
    );
  }
  if (!isChild && config.generative_ui.enabled) {
    capabilities.push((0, import_OpenUI.openUI)({ preload: false, tracing }));
  }
  return capabilities;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  builtinsFromSpec
});
//# sourceMappingURL=builtinsFromSpec.js.map