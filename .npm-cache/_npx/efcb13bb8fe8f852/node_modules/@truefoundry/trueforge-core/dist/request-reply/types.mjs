// src/request-reply/types.ts
import { z } from "zod";
var jsonSchema = z.lazy(() => {
  return z.union([
    z.string(),
    z.number(),
    z.boolean(),
    z.null(),
    z.array(jsonSchema),
    z.record(z.string(), jsonSchema)
  ]);
});
var publishedRequestSchema = z.object({
  replyKey: z.string(),
  path: z.string(),
  body: jsonSchema,
  headers: z.record(z.string(), z.string()).optional()
});
var jsonReplySchema = z.object({
  status: z.number().int().min(200).max(599),
  headers: z.record(z.string(), z.string()).optional(),
  body: jsonSchema
});
export {
  jsonReplySchema,
  publishedRequestSchema
};
//# sourceMappingURL=types.mjs.map