// src/db/postgres/migrations/20260815_000001_sandbox_provider_status.ts
import { sql } from "kysely";
var RELEASE_IMAGE_URI = "tfy.jfrog.io/tfy-images/truefoundry-utils-core-sandbox:029ea5ff6438cf86b79282e087bfc17528067946";
async function up(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.alterTable("sandbox_provider").addColumn("status", "text", (col) => col.notNull().defaultTo("pending")).addColumn("status_reason", "text").addColumn("build_metadata", "jsonb").execute();
  await sql`
    UPDATE sandbox_provider
    SET status = 'ready',
        build_metadata = jsonb_build_object(
          'build_ref', manifest ->> 'snapshot_name',
          'image_uri', ${RELEASE_IMAGE_URI}::text
        )
    WHERE manifest ->> 'snapshot_name' IS NOT NULL
  `.execute(db);
  await db.schema.alterTable("sandbox_provider").alterColumn("status", (col) => col.dropDefault()).execute();
}
async function down(db) {
  await sql`SET LOCAL lock_timeout = '5s'`.execute(db);
  await db.schema.alterTable("sandbox_provider").dropColumn("build_metadata").dropColumn("status_reason").dropColumn("status").execute();
}
export {
  down,
  up
};
//# sourceMappingURL=20260815_000001_sandbox_provider_status.js.map