"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/core/capabilities/builtins/CurrentDateTime.ts
var CurrentDateTime_exports = {};
__export(CurrentDateTime_exports, {
  CURRENT_DATETIME_SERVER_ID: () => CURRENT_DATETIME_SERVER_ID,
  CurrentDateTime: () => CurrentDateTime,
  GET_CURRENT_DATETIME_TOOL_NAME: () => GET_CURRENT_DATETIME_TOOL_NAME,
  currentDateTime: () => currentDateTime
});
module.exports = __toCommonJS(CurrentDateTime_exports);
var import_zod = require("zod");
var import_IMCPServer = require("../../mcp/IMCPServer.js");
var import_LocalToolMCP = require("../../mcp/LocalToolMCP.js");
var CURRENT_DATETIME_SERVER_ID = "current-datetime";
var GET_CURRENT_DATETIME_TOOL_NAME = "get_current_datetime";
var inputSchema = import_zod.z.object({}).strict();
var CurrentDateTime = class extends import_LocalToolMCP.LocalToolMCP {
  name = CURRENT_DATETIME_SERVER_ID;
  displayName = "CurrentDateTime";
  constructor(tracing) {
    super({ tracing });
  }
  tools = [
    (0, import_LocalToolMCP.defineTool)({
      name: GET_CURRENT_DATETIME_TOOL_NAME,
      description: "Returns the current datetime in ISO-8601 (UTC) and unix epoch MS format.",
      schema: inputSchema,
      handler: () => Promise.resolve(this.getCurrentDateTime())
    })
  ];
  getTools() {
    return this.tools;
  }
  // Date.prototype.toISOString() is spec-guaranteed to return UTC (suffix 'Z').
  // Date.prototype.getTime() returns UTC epoch ms regardless of process timezone.
  getCurrentDateTime() {
    const now = /* @__PURE__ */ new Date();
    const payload = {
      unixEpochMS: now.getTime(),
      iso: now.toISOString()
    };
    return (0, import_IMCPServer.toolResultResponse)({ text: JSON.stringify(payload) });
  }
};
function currentDateTime(options) {
  return {
    systemToolSets: [new CurrentDateTime(options.tracing)]
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CURRENT_DATETIME_SERVER_ID,
  CurrentDateTime,
  GET_CURRENT_DATETIME_TOOL_NAME,
  currentDateTime
});
//# sourceMappingURL=CurrentDateTime.js.map