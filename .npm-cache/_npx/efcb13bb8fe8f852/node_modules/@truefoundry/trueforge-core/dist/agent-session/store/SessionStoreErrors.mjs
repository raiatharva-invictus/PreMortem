// src/agent-session/store/SessionStoreErrors.ts
var SessionStoreConflictError = class extends Error {
  constructor(message, options) {
    super(message, options);
    this.name = "SessionStoreConflictError";
  }
};
var SessionStoreNotFoundError = class extends Error {
  constructor(message, options) {
    super(message, options);
    this.name = "SessionStoreNotFoundError";
  }
};
var SessionStoreInvariantError = class extends Error {
  constructor(message, options) {
    super(message, options);
    this.name = "SessionStoreInvariantError";
  }
};
var SessionNotFoundError = class extends SessionStoreNotFoundError {
  session_id;
  constructor(session_id) {
    super(`Session not found: ${session_id}`);
    this.name = "SessionNotFoundError";
    this.session_id = session_id;
  }
};
var TurnNotFoundError = class extends SessionStoreNotFoundError {
  turn_id;
  constructor(turn_id) {
    super(`Turn not found: ${turn_id}`);
    this.name = "TurnNotFoundError";
    this.turn_id = turn_id;
  }
};
var SessionAlreadyExistsError = class extends SessionStoreConflictError {
  session_id;
  constructor(session_id, options) {
    super(`Session already exists: ${session_id}`, options);
    this.name = "SessionAlreadyExistsError";
    this.session_id = session_id;
  }
};
var TurnAlreadyExistsError = class extends SessionStoreConflictError {
  turn_id;
  constructor(turn_id, options) {
    super(`Turn already exists: ${turn_id}`, options);
    this.name = "TurnAlreadyExistsError";
    this.turn_id = turn_id;
  }
};
var PreviousTurnRunningError = class extends SessionStoreConflictError {
  previous_turn_id;
  constructor(previous_turn_id) {
    super(
      `Previous turn ${previous_turn_id} is still running; freezeAndGetTurn must be called before creating a successor`
    );
    this.name = "PreviousTurnRunningError";
    this.previous_turn_id = previous_turn_id;
  }
};
var TurnNotRunningError = class extends SessionStoreConflictError {
  turn_id;
  state;
  constructor(turn_id, state) {
    super(`Turn ${turn_id} is not running (${state.status})`);
    this.name = "TurnNotRunningError";
    this.turn_id = turn_id;
    this.state = state;
  }
};
var InvalidPageTokenError = class extends SessionStoreConflictError {
  token;
  constructor(token) {
    super(`Invalid page token: ${token}`);
    this.name = "InvalidPageTokenError";
    this.token = token;
  }
};
export {
  InvalidPageTokenError,
  PreviousTurnRunningError,
  SessionAlreadyExistsError,
  SessionNotFoundError,
  SessionStoreConflictError,
  SessionStoreInvariantError,
  SessionStoreNotFoundError,
  TurnAlreadyExistsError,
  TurnNotFoundError,
  TurnNotRunningError
};
//# sourceMappingURL=SessionStoreErrors.mjs.map