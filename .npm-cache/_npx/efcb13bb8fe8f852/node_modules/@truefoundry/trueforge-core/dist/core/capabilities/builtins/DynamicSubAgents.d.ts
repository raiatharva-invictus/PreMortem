import type { CallToolRequest } from '@modelcontextprotocol/sdk/types.js';
import { InstructionBuilder } from '../../InstructionBuilder';
import type { InternalToolCallInfo } from '../../llm/LLMTypes';
import { LocalToolMCP, type ToolDefinition } from '../../mcp/LocalToolMCP';
import type { AgentTracing } from '../../tracing/AgentTracing';
import type { AgentCapability } from '../AgentCapability';
export declare const SUB_AGENT_TOOL_NAME = "create_sub_agent";
export declare const SUB_AGENTS_SERVER_ID = "sub_agents";
export declare const SUB_AGENTS_REMINDER_TAG = "sub-agents";
export declare const SUB_AGENT_IDENTITY = "You are the Agent operating as a sub-agent that has been delegated a specific task. The Agent has access to the same tools as the parent agent. The Agent must focus on completing the delegated task and return a concise result. The Agent cannot ask questions to the user.";
export interface ModelSetEntry {
    description: string;
    model_params?: Record<string, unknown> | undefined;
}
export type ModelSetConfig = Record<string, ModelSetEntry>;
export declare function createDynamicSubAgentLargeToolResponseGuidance(): string;
export declare function buildDynamicSubAgentsInstruction(builder: InstructionBuilder, params: {
    sandboxAvailable: boolean;
}): void;
export declare class DynamicSubAgents extends LocalToolMCP {
    readonly name = "sub_agents";
    readonly displayName = "DynamicSubAgents";
    readonly tracingEnabled = false;
    private tools;
    private getCreateSubAgentDescription;
    private getCreateSubAgentTool;
    constructor({ modelSetConfig, tracing }: {
        modelSetConfig?: ModelSetConfig | undefined;
        tracing: AgentTracing;
    });
    protected getTools(): ToolDefinition[];
    toolCallInfo(params: CallToolRequest['params'], _resolveUnderlyingTool?: boolean): Promise<InternalToolCallInfo>;
}
export declare function dynamicSubAgents(options: {
    modelSetConfig?: ModelSetConfig | undefined;
    sandboxAvailable: boolean;
    tracing: AgentTracing;
}): AgentCapability;
//# sourceMappingURL=DynamicSubAgents.d.ts.map