// src/agent-session/builtinsFromSpec.ts
import { askUserQuestion } from "../core/capabilities/builtins/AskUserQuestion.mjs";
import { contextCompaction } from "../core/capabilities/builtins/ContextCompaction.mjs";
import { currentDateTime } from "../core/capabilities/builtins/CurrentDateTime.mjs";
import { dynamicSubAgents } from "../core/capabilities/builtins/DynamicSubAgents.mjs";
import { largeToolResponse } from "../core/capabilities/builtins/LargeToolResponse.mjs";
import { openUI } from "../core/capabilities/builtins/OpenUI.mjs";
function builtinsFromSpec(input) {
  const { spec, definition, isChild, sandboxAvailable, tracing, logger } = input;
  const config = spec.config;
  const capabilities = [currentDateTime({ tracing })];
  if (config.context_management.compaction.enabled) {
    capabilities.push(
      contextCompaction({
        definition,
        compactionThresholdTokens: config.context_management.compaction.compaction_threshold_tokens
      })
    );
  }
  if (!isChild && config.ask_user_questions.enabled) {
    capabilities.push(askUserQuestion());
  }
  const dynamicSubAgentsEnabled = config.dynamic_sub_agents.enabled;
  if (!isChild && dynamicSubAgentsEnabled) {
    capabilities.push(
      dynamicSubAgents({
        sandboxAvailable,
        tracing
      })
    );
  }
  if (config.context_management.large_tool_response.enabled) {
    capabilities.push(
      largeToolResponse({
        dynamicSubAgentsPresent: !isChild && dynamicSubAgentsEnabled,
        logger
      })
    );
  }
  if (!isChild && config.generative_ui.enabled) {
    capabilities.push(openUI({ preload: false, tracing }));
  }
  return capabilities;
}
export {
  builtinsFromSpec
};
//# sourceMappingURL=builtinsFromSpec.mjs.map