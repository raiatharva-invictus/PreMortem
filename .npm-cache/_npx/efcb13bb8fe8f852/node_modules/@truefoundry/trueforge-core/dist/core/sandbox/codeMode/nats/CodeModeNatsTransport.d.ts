import type { Logger } from 'winston';
import type { CodeModeDispatcher } from '../CodeModeDispatcher';
import type { CodeModeClientInstall, CodeModeTransport } from '../CodeModeTransport';
/**
 * NATS Code Mode transport. Session cache + connection-drop invalidate live here
 * (no `whenClosed` on the public interface).
 */
export declare class CodeModeNatsTransport implements CodeModeTransport {
    private readonly resolveHostUrl;
    private readonly sandboxClientNatsUrl;
    private readonly logger;
    private sessionPromise;
    private nc;
    private dispatcher;
    private readonly mcpClientRemotePath;
    private readonly mcpClientPathBinSymlink;
    constructor(params: {
        resolveHostUrl: (sandboxId: string) => Promise<string>;
        sandboxClientNatsUrl?: string;
        logger: Logger;
        /** Provider-owned MCP client layout (absolute or cwd-relative). */
        mcpClientInstall: {
            remotePath: string;
            pathBinSymlink?: string | undefined;
        };
    });
    getClientInstall(): CodeModeClientInstall;
    start(params: {
        codeModeDispatcher: CodeModeDispatcher;
        sandboxId: string;
        requestTimeoutSeconds: number;
    }): Promise<{
        env: Record<string, string>;
    }>;
    stop(): Promise<void>;
    private connectSession;
    private subscribe;
    private handleMessage;
    private encode;
    private decode;
}
//# sourceMappingURL=CodeModeNatsTransport.d.ts.map