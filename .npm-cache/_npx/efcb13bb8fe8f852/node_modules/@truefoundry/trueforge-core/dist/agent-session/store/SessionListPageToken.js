"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/agent-session/store/SessionListPageToken.ts
var SessionListPageToken_exports = {};
__export(SessionListPageToken_exports, {
  SessionListPageCursorSchema: () => SessionListPageCursorSchema,
  decodeSessionListPageToken: () => decodeSessionListPageToken,
  encodeSessionListPageToken: () => encodeSessionListPageToken,
  paginateSessionListRows: () => paginateSessionListRows
});
module.exports = __toCommonJS(SessionListPageToken_exports);
var import_zod = require("zod");
var import_PageToken = require("./PageToken.js");
var SessionListPageCursorSchema = import_zod.z.object({
  updated_at: import_zod.z.iso.datetime({ offset: true }),
  session_id: import_zod.z.string().min(1)
}).strict();
function encodeSessionListPageToken(cursor) {
  return (0, import_PageToken.encodePageToken)(SessionListPageCursorSchema, cursor);
}
function decodeSessionListPageToken(token) {
  if (token === void 0) {
    return void 0;
  }
  return (0, import_PageToken.decodePageToken)(SessionListPageCursorSchema, token);
}
function paginateSessionListRows(rows, limit, getUpdatedAtCursor) {
  const hasMore = rows.length > limit;
  const data = hasMore ? rows.slice(0, limit) : rows;
  const last = data.at(-1);
  return {
    data,
    pagination: {
      limit,
      ...hasMore && last !== void 0 ? {
        next_page_token: encodeSessionListPageToken({
          updated_at: getUpdatedAtCursor(last),
          session_id: last.session_id
        })
      } : {}
    }
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  SessionListPageCursorSchema,
  decodeSessionListPageToken,
  encodeSessionListPageToken,
  paginateSessionListRows
});
//# sourceMappingURL=SessionListPageToken.js.map