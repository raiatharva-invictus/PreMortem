"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/agent-session/store/OffsetPageToken.ts
var OffsetPageToken_exports = {};
__export(OffsetPageToken_exports, {
  OffsetPageCursorSchema: () => OffsetPageCursorSchema,
  decodeOffsetPageToken: () => decodeOffsetPageToken,
  encodeOffsetPageToken: () => encodeOffsetPageToken,
  paginateOffsetRows: () => paginateOffsetRows
});
module.exports = __toCommonJS(OffsetPageToken_exports);
var import_zod = require("zod");
var import_PageToken = require("./PageToken.js");
var OffsetPageCursorSchema = import_zod.z.object({
  offset: import_zod.z.number().int().nonnegative()
}).strict();
function encodeOffsetPageToken(offset) {
  return (0, import_PageToken.encodePageToken)(OffsetPageCursorSchema, { offset });
}
function decodeOffsetPageToken(token) {
  if (token === void 0 || token === "") {
    return 0;
  }
  return (0, import_PageToken.decodePageToken)(OffsetPageCursorSchema, token).offset;
}
function paginateOffsetRows(rows, limit, offset) {
  const hasMore = rows.length > limit;
  const data = hasMore ? rows.slice(0, limit) : rows;
  return {
    data,
    pagination: {
      limit,
      ...hasMore ? { next_page_token: encodeOffsetPageToken(offset + limit) } : {},
      ...offset > 0 ? { previous_page_token: encodeOffsetPageToken(Math.max(0, offset - limit)) } : {}
    }
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  OffsetPageCursorSchema,
  decodeOffsetPageToken,
  encodeOffsetPageToken,
  paginateOffsetRows
});
//# sourceMappingURL=OffsetPageToken.js.map