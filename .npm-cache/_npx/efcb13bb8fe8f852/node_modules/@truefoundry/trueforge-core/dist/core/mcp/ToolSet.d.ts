import type { CallToolRequest } from '@modelcontextprotocol/sdk/types.js';
import type { ApprovalDecision } from '../events/schema';
import type { InternalToolCallInfo } from '../llm/LLMTypes';
import { type CallToolResponse, type IToolSet, type ListToolsResponse, type ToolSource } from './IMCPServer';
import { type ToolSelectorConfig } from './ToolSelectorPolicy';
/**
 * Per-agent {@link IToolSet} view over a shared, policy-free {@link ToolSource}, applying the
 * enable/disable/preload/approval selectors. Many can wrap one source without policy bleed.
 */
export declare class ToolSet implements IToolSet {
    readonly name: string;
    readonly id: string;
    readonly description?: string | undefined;
    readonly preload: boolean;
    readonly hasPreloadedTools: boolean;
    private readonly source;
    private readonly policy;
    constructor(params: {
        source: ToolSource;
        selectors: ToolSelectorConfig;
        preload: boolean;
    });
    getAllowedToolNamesForSandbox(): string[] | undefined;
    listTools(): Promise<ListToolsResponse>;
    callTool(params: CallToolRequest['params'], decision?: ApprovalDecision): Promise<CallToolResponse>;
    toolCallInfo(params: CallToolRequest['params'], resolveUnderlyingTool?: boolean): Promise<InternalToolCallInfo>;
    private assertToolAllowed;
    private buildToolCallInfo;
    private resolveAnnotations;
}
//# sourceMappingURL=ToolSet.d.ts.map