import type { CallToolRequest } from '@modelcontextprotocol/sdk/types.js';
import type { Logger } from 'winston';
import { InstructionBuilder } from '../InstructionBuilder';
import type { InternalToolCallInfo } from '../llm/LLMTypes';
import { type IToolSet } from '../mcp/IMCPServer';
import { LocalToolMCP, type ToolDefinition } from '../mcp/LocalToolMCP';
import type { AgentTracing } from '../tracing/AgentTracing';
export declare const DEFERRED_TOOLS_SERVER_ID = "deferred-tools";
export declare const LIST_TOOLS_NAME = "list_tools";
export declare const GET_TOOL_INFO_NAME = "get_tool_info";
export declare const GET_TOOL_OUTPUT_SCHEMA_NAME = "get_tool_output_schema";
export declare class DeferredTool extends LocalToolMCP {
    readonly name = "deferred-tools";
    readonly displayName = "DeferredTools";
    readonly description = "Deferred tool loading for user MCP servers";
    private readonly serverMap;
    private readonly tools;
    private readonly logger;
    constructor(servers: readonly IToolSet[], options: {
        tracing: AgentTracing;
        logger: Logger;
    });
    buildInstruction(builder: InstructionBuilder): void;
    protected getTools(): ToolDefinition[];
    toolCallInfo(params: CallToolRequest['params'], resolveUnderlyingTool?: boolean): Promise<InternalToolCallInfo>;
    private resolveServer;
    private resolveServerTools;
    private buildTools;
    private buildListTools;
    private buildGetToolInfo;
    private buildGetToolOutputSchema;
    private buildCallTool;
}
//# sourceMappingURL=DeferredTool.d.ts.map