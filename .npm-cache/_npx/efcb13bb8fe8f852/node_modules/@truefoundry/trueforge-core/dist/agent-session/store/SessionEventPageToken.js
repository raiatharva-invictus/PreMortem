"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/agent-session/store/SessionEventPageToken.ts
var SessionEventPageToken_exports = {};
__export(SessionEventPageToken_exports, {
  SessionEventPageCursorSchema: () => SessionEventPageCursorSchema,
  decodeSessionEventPageToken: () => decodeSessionEventPageToken,
  encodeSessionEventPageToken: () => encodeSessionEventPageToken,
  paginateSessionEventRows: () => paginateSessionEventRows
});
module.exports = __toCommonJS(SessionEventPageToken_exports);
var import_zod = require("zod");
var import_PageToken = require("./PageToken.js");
var SessionEventPageCursorSchema = import_zod.z.object({
  last_turn_id: import_zod.z.string().min(1),
  offset: import_zod.z.number().int().nonnegative()
}).strict();
function encodeSessionEventPageToken(cursor) {
  return (0, import_PageToken.encodePageToken)(SessionEventPageCursorSchema, cursor);
}
function decodeSessionEventPageToken(token) {
  return (0, import_PageToken.decodePageToken)(SessionEventPageCursorSchema, token);
}
function paginateSessionEventRows(rows, limit, cursor) {
  const hasMore = rows.length > limit;
  const data = hasMore ? rows.slice(0, limit) : rows;
  return {
    data,
    pagination: {
      limit,
      ...hasMore ? {
        next_page_token: encodeSessionEventPageToken({
          last_turn_id: cursor.last_turn_id,
          offset: cursor.offset + limit
        })
      } : {},
      ...cursor.offset > 0 ? {
        previous_page_token: encodeSessionEventPageToken({
          last_turn_id: cursor.last_turn_id,
          offset: Math.max(0, cursor.offset - limit)
        })
      } : {}
    }
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  SessionEventPageCursorSchema,
  decodeSessionEventPageToken,
  encodeSessionEventPageToken,
  paginateSessionEventRows
});
//# sourceMappingURL=SessionEventPageToken.js.map