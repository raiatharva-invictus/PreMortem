"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/agent-session/store/PageToken.ts
var PageToken_exports = {};
__export(PageToken_exports, {
  decodePageToken: () => decodePageToken,
  encodePageToken: () => encodePageToken
});
module.exports = __toCommonJS(PageToken_exports);
var import_SessionStoreErrors = require("./SessionStoreErrors.js");
function encodePageToken(schema, value) {
  const parsed = schema.parse(value);
  return Buffer.from(JSON.stringify(parsed), "utf8").toString("base64url");
}
function decodePageToken(schema, token) {
  try {
    const value = JSON.parse(Buffer.from(token, "base64url").toString("utf8"));
    return schema.parse(value);
  } catch {
    throw new import_SessionStoreErrors.InvalidPageTokenError(token);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  decodePageToken,
  encodePageToken
});
//# sourceMappingURL=PageToken.js.map