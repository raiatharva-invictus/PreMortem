"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/core/mcp/ClientSideTool.ts
var ClientSideTool_exports = {};
__export(ClientSideTool_exports, {
  ClientSideTool: () => ClientSideTool
});
module.exports = __toCommonJS(ClientSideTool_exports);
var import_NoopAgentTracing = require("../tracing/NoopAgentTracing.js");
var import_LocalToolMCP = require("./LocalToolMCP.js");
var ClientSideTool = class extends import_LocalToolMCP.LocalToolMCP {
  // Public so concrete subclasses (AskUserQuestion, …) remain constructable from
  // factories; abstract class still cannot be instantiated directly.
  constructor() {
    super({ tracing: import_NoopAgentTracing.NOOP_AGENT_TRACING });
  }
  async callTool(params) {
    return {
      clientSideToolRequired: {
        tool_info: await this.toolCallInfo(params)
      }
    };
  }
  toolCallInfo(params, _resolveUnderlyingTool) {
    void _resolveUnderlyingTool;
    return Promise.resolve({
      type: "truefoundry-system",
      original_tool_name: params.name,
      mcp_server_id: this.mcpServerId,
      mcp_server_name: this.name,
      is_client_side: true
    });
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ClientSideTool
});
//# sourceMappingURL=ClientSideTool.js.map