// src/agent-session/store/OffsetPageToken.ts
import { z } from "zod";
import { decodePageToken, encodePageToken } from "./PageToken.mjs";
var OffsetPageCursorSchema = z.object({
  offset: z.number().int().nonnegative()
}).strict();
function encodeOffsetPageToken(offset) {
  return encodePageToken(OffsetPageCursorSchema, { offset });
}
function decodeOffsetPageToken(token) {
  if (token === void 0 || token === "") {
    return 0;
  }
  return decodePageToken(OffsetPageCursorSchema, token).offset;
}
function paginateOffsetRows(rows, limit, offset) {
  const hasMore = rows.length > limit;
  const data = hasMore ? rows.slice(0, limit) : rows;
  return {
    data,
    pagination: {
      limit,
      ...hasMore ? { next_page_token: encodeOffsetPageToken(offset + limit) } : {},
      ...offset > 0 ? { previous_page_token: encodeOffsetPageToken(Math.max(0, offset - limit)) } : {}
    }
  };
}
export {
  OffsetPageCursorSchema,
  decodeOffsetPageToken,
  encodeOffsetPageToken,
  paginateOffsetRows
};
//# sourceMappingURL=OffsetPageToken.mjs.map