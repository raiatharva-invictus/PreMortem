// src/core/util/abort.ts
function onSignalAbort(signal, callback) {
  if (!signal) {
    return;
  }
  if (signal.aborted) {
    callback();
    return;
  }
  signal.addEventListener("abort", callback, { once: true });
}
export {
  onSignalAbort
};
//# sourceMappingURL=abort.mjs.map