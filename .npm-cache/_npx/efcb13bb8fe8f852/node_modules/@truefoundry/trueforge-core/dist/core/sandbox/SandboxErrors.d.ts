export declare abstract class SandboxError extends Error {
    /**
     * Every sandbox failure is caused by the request itself — a bad path, a missing
     * or oversized file — so each subclass carries the status a host should reply with.
     * Listing the codes in use rather than `number` lets a typed route return this directly; adding
     * a subclass with a new code means widening this union and declaring it on the route.
     */
    abstract readonly statusCode: 400 | 403 | 404 | 410 | 413;
}
export declare class SandboxFileNotFoundError extends SandboxError {
    readonly statusCode = 404;
    constructor(path: string);
}
export declare class SandboxNotAvailableError extends SandboxError {
    readonly statusCode = 410;
    constructor(sandboxId: string);
}
export declare class SandboxPathIsDirectoryError extends SandboxError {
    readonly statusCode = 400;
    constructor(path: string);
}
export declare class SandboxFileTooLargeError extends SandboxError {
    readonly statusCode = 413;
    readonly fileSize: number;
    readonly maxSize: number;
    constructor(path: string, fileSize: number, maxSize: number);
}
/** Daytona/TFY raw ids are `tenantName.<uuid>`. Fancy ids and local paths must not be passed here. */
export declare function validateSandboxOwnedByTenant(params: {
    sandboxId: string;
    tenantName: string;
}): void;
export declare function validateNoPathTraversal(path: string): void;
//# sourceMappingURL=SandboxErrors.d.ts.map