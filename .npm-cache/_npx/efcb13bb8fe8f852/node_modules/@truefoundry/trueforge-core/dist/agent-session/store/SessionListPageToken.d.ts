import { z } from 'zod';
import type { TokenPagination } from '../schemas/pagination';
/**
 * Keyset cursor for `listSessions` (`updated_at`, `session_id`).
 * Opaque on the wire; replaces offset tokens for this list only.
 *
 * `updated_at` must retain the store's full precision (Postgres: microseconds via
 * `to_char`; JS Date / SQLite ISO text: milliseconds). Do not round through
 * `Date` before encode/compare — equality on the cursor must match the DB value.
 */
export declare const SessionListPageCursorSchema: z.ZodObject<{
    updated_at: z.ZodISODateTime;
    session_id: z.ZodString;
}, z.core.$strict>;
export type SessionListPageCursor = z.infer<typeof SessionListPageCursorSchema>;
export declare function encodeSessionListPageToken(cursor: SessionListPageCursor): string;
export declare function decodeSessionListPageToken(token: string | undefined): SessionListPageCursor | undefined;
export declare function paginateSessionListRows<T extends {
    session_id: string;
}>(rows: T[], limit: number, getUpdatedAtCursor: (row: T) => string): {
    data: T[];
    pagination: TokenPagination;
};
//# sourceMappingURL=SessionListPageToken.d.ts.map