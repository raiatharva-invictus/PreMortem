"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/agent-session/store/assertCreateTurnThreadDelta.ts
var assertCreateTurnThreadDelta_exports = {};
__export(assertCreateTurnThreadDelta_exports, {
  assertCreateTurnThreadDelta: () => assertCreateTurnThreadDelta
});
module.exports = __toCommonJS(assertCreateTurnThreadDelta_exports);
var import_SessionStoreErrors = require("./SessionStoreErrors.js");
function assertCreateTurnThreadDelta(input) {
  const newThreadIds = /* @__PURE__ */ new Set();
  for (const nt of input.new_threads) {
    if (input.previousThreadIds.has(nt.thread_id) || newThreadIds.has(nt.thread_id)) {
      throw new import_SessionStoreErrors.SessionStoreInvariantError(
        `new_threads must only contain threads absent on the previous turn; thread '${nt.thread_id}' already exists`
      );
    }
    newThreadIds.add(nt.thread_id);
  }
  const knownThreadIds = new Set(input.previousThreadIds);
  for (const threadId of newThreadIds) {
    knownThreadIds.add(threadId);
  }
  for (const append of input.new_context_appends) {
    if (!knownThreadIds.has(append.thread_id)) {
      throw new import_SessionStoreErrors.SessionStoreInvariantError(`new_context_appends references unknown thread ${append.thread_id}`);
    }
  }
  const seenCapabilityThreads = /* @__PURE__ */ new Set();
  for (const capability of input.capability_states) {
    if (!knownThreadIds.has(capability.thread_id)) {
      throw new import_SessionStoreErrors.SessionStoreInvariantError(`capability_states references unknown thread ${capability.thread_id}`);
    }
    if (seenCapabilityThreads.has(capability.thread_id)) {
      throw new import_SessionStoreErrors.SessionStoreInvariantError(`capability_states contains duplicate thread ${capability.thread_id}`);
    }
    seenCapabilityThreads.add(capability.thread_id);
  }
  for (const threadId of knownThreadIds) {
    if (!seenCapabilityThreads.has(threadId)) {
      throw new import_SessionStoreErrors.SessionStoreInvariantError(`capability_states is missing thread ${threadId}`);
    }
  }
  return knownThreadIds;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  assertCreateTurnThreadDelta
});
//# sourceMappingURL=assertCreateTurnThreadDelta.js.map