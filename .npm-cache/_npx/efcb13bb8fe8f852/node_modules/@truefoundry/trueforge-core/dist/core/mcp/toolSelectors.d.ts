import type { ToolAnnotations } from '@modelcontextprotocol/sdk/types.js';
/** Tags allowed on `enable_tools`, `disable_tools`, and `preload_tools`. */
export declare const TOOLS_SELECTOR_TAGS: readonly ["@all", "@read-only"];
/** Tags allowed on `require_approval_for_tools`. */
export declare const REQUIRE_APPROVAL_TOOLS_SELECTOR_TAGS: readonly ["@all", "@write", "@destructive"];
export declare const DEFAULT_ENABLE_TOOLS: string[];
export declare const DEFAULT_DISABLE_TOOLS: string[];
export declare const DEFAULT_PRELOAD_TOOLS: string[];
export declare const DEFAULT_REQUIRE_APPROVAL_FOR_TOOLS: string[];
/** True if the tool matches any selector (tag or literal name). */
export declare function toolMatchesAnySelector(toolName: string, annotations: ToolAnnotations | undefined, selectors: readonly string[] | undefined): boolean;
/**
 * Whether the tool requires human approval before execution.
 * Matches `@write`, `@destructive`, `@all`, or explicit tool names against annotations.
 * Unannotated tools are exempt unless named in `require_approval_for_tools` or covered by `@all`.
 */
export declare function toolRequiresApproval(toolName: string, annotations: ToolAnnotations | undefined, selectors: readonly string[] | undefined): boolean;
/** True if the tool is enabled and not disabled. */
export declare function isToolAllowed(params: {
    toolName: string;
    annotations: ToolAnnotations | undefined;
    enableSelectors: readonly string[];
    disableSelectors: readonly string[];
}): boolean;
/** Non-tag names from a selector list (for upfront validation / sandbox allow-lists). */
export declare function literalToolNames(selectors: readonly string[] | undefined): string[];
export declare function selectorsIncludeAll(selectors: readonly string[] | undefined): boolean;
//# sourceMappingURL=toolSelectors.d.ts.map