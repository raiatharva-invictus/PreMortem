"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/core/mcp/toolSelectors.ts
var toolSelectors_exports = {};
__export(toolSelectors_exports, {
  DEFAULT_DISABLE_TOOLS: () => DEFAULT_DISABLE_TOOLS,
  DEFAULT_ENABLE_TOOLS: () => DEFAULT_ENABLE_TOOLS,
  DEFAULT_PRELOAD_TOOLS: () => DEFAULT_PRELOAD_TOOLS,
  DEFAULT_REQUIRE_APPROVAL_FOR_TOOLS: () => DEFAULT_REQUIRE_APPROVAL_FOR_TOOLS,
  REQUIRE_APPROVAL_TOOLS_SELECTOR_TAGS: () => REQUIRE_APPROVAL_TOOLS_SELECTOR_TAGS,
  TOOLS_SELECTOR_TAGS: () => TOOLS_SELECTOR_TAGS,
  isToolAllowed: () => isToolAllowed,
  literalToolNames: () => literalToolNames,
  selectorsIncludeAll: () => selectorsIncludeAll,
  toolMatchesAnySelector: () => toolMatchesAnySelector,
  toolRequiresApproval: () => toolRequiresApproval
});
module.exports = __toCommonJS(toolSelectors_exports);
var TOOL_TAG_ALL = "@all";
var TOOL_TAG_READ_ONLY = "@read-only";
var TOOL_TAG_WRITE = "@write";
var TOOL_TAG_DESTRUCTIVE = "@destructive";
var TOOL_TAGS = [TOOL_TAG_ALL, TOOL_TAG_READ_ONLY, TOOL_TAG_WRITE, TOOL_TAG_DESTRUCTIVE];
var TOOLS_SELECTOR_TAGS = [TOOL_TAG_ALL, TOOL_TAG_READ_ONLY];
var REQUIRE_APPROVAL_TOOLS_SELECTOR_TAGS = [TOOL_TAG_ALL, TOOL_TAG_WRITE, TOOL_TAG_DESTRUCTIVE];
var DEFAULT_ENABLE_TOOLS = [TOOL_TAG_ALL];
var DEFAULT_DISABLE_TOOLS = [];
var DEFAULT_PRELOAD_TOOLS = [];
var DEFAULT_REQUIRE_APPROVAL_FOR_TOOLS = [TOOL_TAG_WRITE, TOOL_TAG_DESTRUCTIVE];
function isToolTag(selector) {
  return TOOL_TAGS.includes(selector);
}
function isReadOnly(annotations) {
  return annotations?.readOnlyHint === true;
}
function isWrite(annotations) {
  return annotations?.readOnlyHint === false && annotations.destructiveHint !== true;
}
function isDestructive(annotations) {
  return annotations?.destructiveHint === true;
}
function toolMatchesTag(tag, annotations) {
  switch (tag) {
    case TOOL_TAG_ALL:
      return true;
    case TOOL_TAG_READ_ONLY:
      return isReadOnly(annotations);
    case TOOL_TAG_WRITE:
      return isWrite(annotations);
    case TOOL_TAG_DESTRUCTIVE:
      return isDestructive(annotations);
  }
}
function toolMatchesAnySelector(toolName, annotations, selectors) {
  if (!selectors?.length) {
    return false;
  }
  for (const selector of selectors) {
    if (isToolTag(selector)) {
      if (toolMatchesTag(selector, annotations)) {
        return true;
      }
    } else if (selector === toolName) {
      return true;
    }
  }
  return false;
}
function toolRequiresApproval(toolName, annotations, selectors) {
  if (!selectors?.length) {
    return false;
  }
  return toolMatchesAnySelector(toolName, annotations, selectors);
}
function isToolAllowed(params) {
  const { toolName, annotations, enableSelectors, disableSelectors } = params;
  if (!toolMatchesAnySelector(toolName, annotations, enableSelectors)) {
    return false;
  }
  if (toolMatchesAnySelector(toolName, annotations, disableSelectors)) {
    return false;
  }
  return true;
}
function literalToolNames(selectors) {
  if (!selectors) {
    return [];
  }
  return selectors.filter((s) => !isToolTag(s));
}
function selectorsIncludeAll(selectors) {
  return !!selectors?.includes(TOOL_TAG_ALL);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  DEFAULT_DISABLE_TOOLS,
  DEFAULT_ENABLE_TOOLS,
  DEFAULT_PRELOAD_TOOLS,
  DEFAULT_REQUIRE_APPROVAL_FOR_TOOLS,
  REQUIRE_APPROVAL_TOOLS_SELECTOR_TAGS,
  TOOLS_SELECTOR_TAGS,
  isToolAllowed,
  literalToolNames,
  selectorsIncludeAll,
  toolMatchesAnySelector,
  toolRequiresApproval
});
//# sourceMappingURL=toolSelectors.js.map