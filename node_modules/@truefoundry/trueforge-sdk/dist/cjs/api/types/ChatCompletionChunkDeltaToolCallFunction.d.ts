export interface ChatCompletionChunkDeltaToolCallFunction {
    /** Partial or complete JSON arguments string. */
    arguments?: string;
    /** Partial or complete function name. */
    name?: string;
}
